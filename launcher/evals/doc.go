// Package evals contains the opt-in release-contract evaluation suite.
//
// The package declaration remains visible without the eval build tag so
// `go test ./...` and `go test -race ./...` also work with newer Go versions.
package evals
